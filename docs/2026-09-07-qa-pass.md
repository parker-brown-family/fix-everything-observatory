# QA pass — v2 (2026-09-07)

Driven in a real Chrome via the browser tools against the live server at
`http://127.0.0.1:4517/app/swarm.html`, backed by a real mine of `omacom/omarchy`.

## What was verified

| Check | Result |
|---|---|
| Page loads, no console errors | ✅ clean (checked with onlyErrors) |
| Live mine populates | ✅ 300 artifacts, 200 comments, 141 events, api 24/60 remaining |
| Agent smell detected in the wild | ✅ 45 smell + 3 hint (real AI trailers in omarchy PRs) |
| Smell motes visually distinct | ✅ violet + stronger glow; confirmed by zoom |
| AGENTIC scope filters to agent tickets only | ✅ only violet/yellow motes remain (`s` key) |
| Ticket inspector = full story | ✅ #10587: opened → review requested ×2 → 2 real comments, with snippets |
| Stats bar tracks cursor | ✅ 8 counters, tween-animated on replay |
| CFD renders 3 stacked bands | ✅ merged/closed/open; band gap = WIP |
| Replay (space) animates history | ✅ counters + swarm + CFD + feed all move with T |
| Live feed surfaces recent activity | ✅ newest 7, click-through to inspector |
| Simulation mode | ✅ watermarked, healing arc visible, own stream |
| Fog for uninspected history | ✅ hatched left margin + coverage chips |
| Tray icon = official mark | ✅ green chip glyph, matches bar menu button |

## Honest-data behaviours confirmed (house rule: unknown ≠ zero)

- Live CFD is almost entirely the amber "still open" band — correct: omarchy
  reports 3381 open issues; the swarm accumulates faster than it settles. The
  page shows the true shape, not a flattering one.
- MARKER scope is still an honest zero with explanation (no wild ticket carries
  the fix-event stamp yet).
- Coverage chips name the fetched horizon; comments/events windows labelled.
- API budget shown on the rail; miner skips a cycle below 8 remaining.

## Known limits (tracked, not hidden)

- Reopen/close events are only those within the fetched events window (2 pages
  unauth). Labelled "in window"; older transitions are unknown, never zero.
- Comment snippets are truncated + code-stripped server-side (privacy + size).
- No auth by default → 3/2/2 page horizon. `GITHUB_TOKEN` raises it to 10/3/3.

## Follow-ups worth a GitHub issue when this gets a repo

1. Per-ticket lazy enrichment (full timeline) on inspector open, budget-gated.
2. Marker emission in the one-click fixer (flips MARKER scope live).
3. Deploy path: publish `manifest.js` beside the page on wecanfixeverything.com.
